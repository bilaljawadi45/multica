# Stand-alone project — never merge into the parent pnpm workspace.
# This package is the sandbox Playwright harness; the parent repo's workspace
# only covers apps/* and packages/*, but having .npmrc here guarantees no
# future drift pulls us in.
ignore-workspace=true
